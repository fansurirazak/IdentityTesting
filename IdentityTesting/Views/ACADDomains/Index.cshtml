@model IEnumerable<IdentityTesting.Models.ACADDomain>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table" id="woopwoop">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ACADDomainName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ACADDomainName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ACADDomainID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ACADDomainID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ACADDomainID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
